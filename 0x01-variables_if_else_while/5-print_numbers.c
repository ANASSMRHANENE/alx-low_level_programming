#include <stdio.h>
/**
 * main - shoing numbers from 0 to 10
 * Return: 0(Success)
 */
int main(void)
{
int number
for (number = 0; number <= 10 ; number++)
{
	printf("%d", number);
}
printf("\n");
return (0);
}
