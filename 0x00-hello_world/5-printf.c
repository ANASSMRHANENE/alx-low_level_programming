#include <stdio.h>
/**
 * main - this main is about showing with proper grammar,
 * but the outcome is a piece of art,message
 * Return: 0(Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
