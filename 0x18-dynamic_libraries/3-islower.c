#include "main.h"
/**
 * _islower - a project about veryfing if a
 * letter is lowercase
 * @c: is a parametre
 * Return: 0(SUCCESS)
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
		return (1);
	}
return (0);
}
